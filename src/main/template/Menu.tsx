import * as React from 'react';
import SideBar from "./SideBar";
import MenuBar from "./MenuBar";

const Menu = () => (
    <div>
        <MenuBar/>
        <SideBar/>
    </div>
);

export default Menu;